# 消费者心智模拟系统（MindSim）使用手册

面向市场 / 产品同学。按本文操作即可完成：**生成消费者 → 测 Campaign → 读结果 → 真实验证复盘**。

默认本地地址：http://127.0.0.1:8000  
（若已部署腾讯云，则为服务器 IP / 域名，用法相同。）

---

## 1. 这个系统是干什么的

用 **JTBD（Jobs to be Done）** 模拟一批「呼吸机相关」消费者：

- 每个人有自己的**任务**（例如修复亲密关系、搞清严不严重）、**卡点步骤**、**最在乎的结果**；
- 你输入一句营销话术，系统给出每人的：
  - **判定**：推进 / 犹豫 / 无关 / 拒绝  
  - **意愿分**：1～10  
  - **第一人称反馈** + 已改善 / 未解决的关注点  
- 目的不是「替代真实调研」，而是快速筛话术、看差异、带着问题去对真实证据。

---

## 2. 怎么打开

任选其一：

1. **双击**桌面 `MindSim` 快捷方式，或项目目录下的 `MindSim.exe`（须与 `backend` / `frontend` / `data` 同级）  
2. 或在项目目录运行：`.\start.ps1`  

浏览器打开 **http://127.0.0.1:8000**。  
关掉黑色命令窗口 = 停止服务。

> 若页面打开却是别的工具（例如医院渠道判断），说明 **8000 端口被占用**，先关掉那个程序再启动 MindSim。

---

## 3. 页面结构（三个入口）

| 入口 | 地址 | 给谁用 |
|------|------|--------|
| **工作台** | `/` | 市场日常：生成人、测话术、看结果、打验证 |
| **任务后台** | `/jobs` | 看 Job / 子任务 / Outcome 字典 |
| **维度后台** | `/admin` | 启停决策维度、调权重（偏配置） |

日常 90% 时间在**工作台**即可。

---

## 4. 推荐主流程（10 分钟跑通）

```text
① 看清「库中已有」人数
②（可选）生成一批新消费者
③ 点左侧人名看画像（不必先测）
④ 勾选「可复现模式」← 做决策对比时强烈推荐
⑤ 填入话术 → 运行测试
⑥ 看矩阵 + 逐人卡片
⑦ 对照真实证据，点「真实验证」打标（复盘）
```

### 4.1 库中人数 vs 下次生成人数

- **库中已有消费者**：打开页面时已经在库里的人（上次生成留下的）。  
- **下次生成人数**：你点「生成消费者心智」时**新造多少人**（会**覆盖**旧名单）。  

两者不是一回事。不要因为库里是 10 人、输入框默认也是 10，就以为「还没点生成却已经生成了」。

### 4.2 生成消费者

1. 设「下次生成人数」（建议先 10）。  
2. （可选）点选「从什么问题开始」：心血管风险、伴侣睡眠受损等（可多选，选中会带 ✓）。  
3. （可选）勾选 **LLM 生成**：文案更丰富，但更慢、需 API Key；不勾选则用规则原型，更快、更稳。  
4. 点 **生成消费者心智**，等进度提示结束。  

生成后：左侧出现列表；点人名即可看右侧画像。

### 4.3 看懂画像（点人名）

画像里重点看：

| 区块 | 含义 |
|------|------|
| 年龄 / 城市 / 职业 / 分群 / 角色 | 这是谁、站在什么立场决策 |
| 起点情境 / 正在做的事 / 当前卡在 | TA 的任务与卡点 |
| TA 最在乎什么 | 色条：在乎高、满意低 = **缺口大**，话术应优先打这里 |
| 专业样本 | 如呼吸科医生：KOL 型样本，不宜当普通消费者占比 |
| 室友影响 | 被动受害者叙事，不是本人打鼾求治 |
| 经历时间线 | 勾选「消费者有记忆」并测过后，这里会记变化 |

### 4.4 测 Campaign（核心）

1. 在文本框输入话术，或点 **填入示例**。  
2. 选模式（见下一节）。  
3. 点 **运行测试**。  
4. 下方出现：  
   - **对照矩阵**（行=人，列=关注点：改善 / 未解）  
   - **逐人反馈卡**（判定、意愿、口语反馈、💭 解读）

点击矩阵行或结果卡 → 右侧打开对应画像，方便对照。

---

## 5. 三种测试模式怎么选

工作台有三个勾选项，**可复现** 与 **独立思考（LLM）** 互斥（勾一个会取消另一个）。

| 模式 | 怎么勾 | 适合什么 | 注意 |
|------|--------|----------|------|
| **可复现模式（固定模式）** | 勾「可复现模式」 | **比话术、做决策、写报告、复盘** | 同一话术 + 同一批人 → 判定与意愿分锁定；文案偏规则口语 |
| **独立思考（LLM）** | 勾「独立思考」，不勾可复现 | 听「更像人」的反馈、发散洞察 | 文案可能略有波动；判定仍会与规则门控对齐 |
| **规则静态（默认可关 LLM）** | 两个都不勾 | 快速试跑 | 等同可复现的规则引擎路径 |
| **消费者有记忆** | 额外勾选 | 模拟「听过多次话术」的累积 | 测后写入经历时间线；对比话术时建议先清记忆或不用记忆 |

### 5.1 可复现模式（你说的「固定模式」）——详解

**什么时候必须用**

- A 话术 vs B 话术比谁更好  
- 同一句话术跑两遍，检查系统是否稳  
- 给老板 / 代理商看「可核对」的结果  
- 做灵敏度测试：好话术 / 跑题 / 劝阻  

**操作**

1. 勾选 **可复现模式**（独立思考会自动取消）。  
2. 确认库中是你要测的那批人（必要时先生成并记下人数）。  
3. 输入话术 → **运行测试**。  
4. 再跑一遍：判定、意愿分、反馈应一致。  

**结果怎么读（门控直觉）**

- **推进**：话术打到了 TA 最急的缺口，且高重要度顾虑覆盖够 → 可以往下一步走。  
- **犹豫**：沾边，但最卡的点还没说透。  
- **无关**：完全跑题（例如爬山野餐），跟治疗决策不沾边。  
- **拒绝**：劝阻 / 反向（例如「不用买、忍忍就行」），在往回推人。  

意愿分大致：推进偏高（约 6～10），犹豫居中（约 3～6），拒绝/无关偏低。  
**核心顾虑未解决时，不会出现「满分推进」**——这是刻意设计。

### 5.2 独立思考（LLM）

- 需要本机 / 服务器配置了 DeepSeek 等 API Key。  
- 反馈更口语、更个性化；仍建议关键决策用可复现模式复核一遍。

### 5.3 消费者有记忆

勾选后，每次测试会把经历写入该人的时间线（步骤变化等）。  

**复盘场景示例**

1. 第一轮：可复现 + 无记忆，测话术 A。  
2. 勾选有记忆，再测话术 B（模拟「上次听过 A」）。  
3. 打开画像看「经历时间线」是否合理。  
4. 对比结束后可用 **清除全部记忆** 或单人「清除记忆」复位。

---

## 6. 怎么读测试结果

### 6.1 顶部提示

可能出现「维度：… · 识别到的助力点：试戴/试用」等——表示话术里识别到的卖点类型（中文），不是给研发看的内部代码。

### 6.2 矩阵

- 列名应是中文关注点（如「戴得住用不下去」「伴侣睡眠/关系」）。  
- **改善** = 这句话术帮到了这项；**未解** = 仍卡着。  
- 点一行 → 打开该人画像。

### 6.3 反馈卡字段

| 字段 | 含义 |
|------|------|
| 推进 / 犹豫 / 无关 / 拒绝 | 决策标签 |
| 意愿 x/10 | 推进意愿强度 |
| 正文 | 第一人称反应 |
| 💭 | 用人话解释「帮到了什么 / 还卡什么」 |
| 已改善 / 未解决 | 与矩阵一致，中文关注点 |
| 规则 / LLM · 静态 / 有记忆 | 本次用的模式 |

---

## 7. 真实验证与复盘（怎么用）

「真实验证」**不是**系统自动判的好坏，而是你把**真实世界证据**回写到模拟结果上。

每张结果卡底部三个按钮：

| 按钮 | 什么时候点 |
|------|------------|
| **已验证** | 模拟方向与真实一致（销售/回访/试用反馈对得上） |
| **未验证** | 还没对过真实数据，先占位 |
| **方向相反** | 模拟和真实相反（例如系统说推进，真实更抵触） |

点击后可填一句短证据（可选），会记入该消费者记忆，便于以后在「经历时间线」里复盘。

### 7.1 推荐复盘节奏

1. **可复现模式**测完话术，导出或截图矩阵 + 关键人物卡。  
2. 对照真实渠道：哪几类人真的转化了 / 拒绝了。  
3. 对关键人物点 **已验证** 或 **方向相反**，写清证据。  
4. 若多轮测试：勾选 **有记忆**，看时间线是否符合「听过多次」的真实路径。  
5. 阶段性复盘后：**下载记忆**备份；需要干净对照时 **清除全部记忆**。

### 7.2 记忆管理（生成栏下方）

| 按钮 | 作用 |
|------|------|
| 下载记忆 | 导出 JSON 备份 |
| 上传记忆 | 覆盖导入（慎用） |
| 清除全部记忆 | 清空经历，阶段复位 |
| 画像里「清除记忆」 | 只清当前这一人 |

---

## 8. 标签口径速查（避免看懵）

三套标签不要混：

| 标签 | 回答的问题 | 例子 |
|------|------------|------|
| **分群**（左侧筛选） | 为什么买 | 关系驱动、健康焦虑自用、经济受限… |
| **决策阶段**（左侧筛选） | 买到哪一步 | 未察觉 → 确诊 → 纠结 → 购买 → 适应… |
| **起点情境**（生成栏） | 生成时偏重哪类触发 | 心血管风险、伴侣睡眠受损… |

筛选后，列表标题会显示「显示 9 / 共 10 人」——说明筛选生效。

---

## 9. 灵敏度自测（建议定期做）

用**同一批人 + 可复现模式**，跑三句：

| 类型 | 示例话术 | 期望大致分布 |
|------|----------|--------------|
| 好话术 | 「填入示例」（试戴+医院同款+国补…） | 有人推进、有人犹豫，有梯度 |
| 跑题 | 今天天气不错，一起去爬山野餐 | 多为**无关** |
| 劝阻 | 打呼噜不是大毛病，不用花冤枉钱买呼吸机，忍忍就行 | 多为**拒绝** |

若好话术人人满分、或跑题判成拒绝、劝阻判成无关 → 门控异常，需找研发排查。

---

## 10. 任务后台 / 维度后台（简要）

- **任务后台 `/jobs`**：查主任务、步骤路径、Outcome 定义；写话术时用来对齐「该打哪个关注点」。  
- **维度后台 `/admin`**：启停维度、调权重；影响识别与计分，改前建议先备份、改后用可复现模式回归灵敏度三句。

---

## 11. 常见问题

**Q：点生成后列表还是空的？**  
强刷（Ctrl+F5）。若控制台有 `SyntaxError`，把报错发研发。确认 `app.js?v=` 为最新版本号。

**Q：矩阵表头又是 O3/O8？**  
应显示中文。强刷；仍不行则把当前 `app.js` 版本号发给研发。

**Q：LLM 很慢 / 报错？**  
不勾 LLM，用可复现或规则即可。配置 Key 见 `README.md` / `.env.example`。

**Q：和上次结果对不上？**  
检查是否勾了可复现；库中人是否被重新生成覆盖；是否开了记忆。

**Q：端口被占用？**  
关掉占用 8000 的其他程序，或改用其他端口启动（见 README）。

---

## 12. 市场部一页纸备忘

1. 做决策 → **必须勾可复现模式**。  
2. 先看每人「最在乎且缺口大」的项，再看话术有没有打到。  
3. 矩阵看结构，卡片看态度，画像看人设是否自洽。  
4. 真实验证用来**纠偏模拟**，不是再跑一次 AI。  
5. 定期用「好 / 跑题 / 劝阻」三句话做灵敏度体检。  

---

*文档版本：与 MindSim JTBD v3 工作台对齐（含可复现模式、真实验证、记忆管理）。*  
*若界面文案与本文不一致，以当前页面为准，并同步更新本文。*
